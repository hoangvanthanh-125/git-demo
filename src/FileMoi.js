import React from 'react';
import PropTypes from 'prop-types';

FileMoi.propTypes = {
  
};
const renderList = ()=> {
  console.log('hihi');
}
function FileMoi(props) {
  return (
    <div>
     {renderList()}
    </div>
  );
}

export default FileMoi;
